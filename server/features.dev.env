# Feature flags 
ENABLE_SMS=0
# The lowest version the server will accept 
# before returning an update client error.
LOWEST_COMPATIBLE_CLIENT_VERSION=0
# Cloud project
CLOUD_PROJECT_ID=resonates-xyz
# Database 
CLOUD_DATABASE_ID=rxyz-db
# Cache bucket 
CLOUD_STORAGE_BUCKET_CACHE=rxyz-fetch-cache
# Images bucket 
CLOUD_STORAGE_BUCKET_IMAGES=rxyz-images
# Secrets key version
CLOUD_SECRETS_KEY="projects/433084662574/secrets/app_secrets/versions/2"